import { Typography } from "@mui/material";

export const ContentErrorText = () => {
  return (
    <Typography variant="h2" color="error" component="div" align="center">
      Kunne ikke laste innhold!
    </Typography>
  );
};

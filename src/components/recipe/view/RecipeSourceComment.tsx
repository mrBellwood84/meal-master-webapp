import { Box, Typography } from "@mui/material";

export const RecipeSourceComment = () => {
  return (
    <Box sx={{ p: 2, ml: 1 }}>
      <Typography variant="subtitle2">Kilde: Egen oppskrift</Typography>
    </Box>
  );
};
